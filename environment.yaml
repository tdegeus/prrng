channels:
  - conda-forge
dependencies:
  # dependencies
  - xtensor
  - gcem

  # build tools
  - cmake
  - ninja
  - setuptools_scm

  # Python API
  - python
  - numpy
  - pyxtensor

  # to run tests
  - catch2

  # build docs
  - doxygen
  - mathjax
